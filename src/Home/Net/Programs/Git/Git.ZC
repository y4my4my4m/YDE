// Git client for YKD, by y4my4m. Public Domain.
//
//   #include "C:/Home/Net/Programs/SSH/SSH"   // before Git, to enable ssh urls
//   #include "C:/Home/Net/Programs/Git/Git"
//   GitLsRemote("https://github.com/user/repo");         // list refs
//   GitClone("https://github.com/user/repo");            // -> ./repo
//   GitClone("git@github.com:user/repo.git");            // -> ./repo
//   GitClone("git@github.com:user/repo.git", "MyName");  // -> ./MyName
//
// Requires the Net stack (HTTP/TLS) to be loaded.

Cd(__DIR__);;

#include "GitSHA1"
#include "GitProto"
#include "GitPack"    // inflate + delta resolution + working-tree checkout

#ifdef SSHTransportConnect
#include "GitSSH"     // ssh transport for GitClone, when the SSH client is loaded
#endif
