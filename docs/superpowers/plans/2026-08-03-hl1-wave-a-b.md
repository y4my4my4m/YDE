# HL1 Wave A Implementation Plan

> **For agentic workers:** Use executing-plans or subagent-driven-development. Checkbox steps.

**Goal:** Campaign-playable gaps from `docs/superpowers/specs/2026-08-03-hl1-wave-a-design.md`.

**Architecture:** Extend existing `hl_proj[]` for AI shots; wire monsterclip into slidebox traces; give fauna AI kinds; MakeDormant flag; tracktrain hull fix; HUD/FX wiring.

**Tech Stack:** ZealC in `src/Apps/HL1`. SDK cite `file:line`. Hybrid C fidelity.

**Global Constraints:**
- No new second projectile pool — only `hl_proj[]` / `HLProjSpawn`.
- Include order: publish globals before later files need them (`RunLib.ZC` order).
- Do not rewrite working `SquadRecruit`; fix schedule/slot use if grunts still blob.
- Stale comments (zoom FOV, player dmg) fixed in Task 7.

---

### Task 1: AI projectile kinds + anim events

**Files:**
- Modify: `src/Apps/HL1/HLWeapon.ZC` (`HLPRJ_*`, `HLProjFrame`, spawn helpers)
- Modify: `src/Apps/HL1/HLAI.ZC` (`HLAIMonAnimEvent` ~7223+)
- Modify: `src/Apps/HL1/HLSave.ZC` if kind enum persists

**Interfaces:**
- Produces: `HLPRJ_SPIT`, `HLPRJ_CTRLBALL`, `HLPRJ_AIGRENADE` (HORNET exists); `HLProjSpawnAI(kind, origin, vel, dmg, bits, owner)`
- Consumes: `HLProjAlloc` / `HLProjSpawn` / `HLDmgHurt`

- [ ] **Step 1:** Add kinds after `HLPRJ_HORNET`; bump `HLPRJ_KINDS`; model/sprite paths from SDK (spit sprite, controller ball, grenade model).
- [ ] **Step 2:** In `HLProjFrame`, integrate new kinds (move, touch hull, damage, free). Mirror existing HORNET/TIMED patterns.
- [ ] **Step 3:** Bullsquid code1 → spawn spit toward enemy (not growl-only).
- [ ] **Step 4:** Hgrunt grenade events 7/8/9 → `HLPRJ_AIGRENADE` timed.
- [ ] **Step 5:** Islave zap / agrunt hornet / controller ball → spawn HORNET or CTRLBALL instead of `HLAIShootOnce` where SDK uses projectiles.
- [ ] **Step 6:** Save/load new kinds safely (unknown → FREE).
- [ ] **Step 7:** Commit `feat(hl1): AI projectiles on hl_proj pool`

---

### Task 2: Monsterclip + monster traces

**Files:**
- Modify: `HLEntity.ZC` (`func_monsterclip` spawn ~11905, clip hooks)
- Modify: `HLClip.ZC` / slidebox path
- Modify: `HLAI.ZC` (`HLAIStepTo` / move traces)

- [ ] **Step 1:** Define `HLFL_MONSTERCLIP` (or reuse entity flag bit); spawn `func_monsterclip` as brush solid **only** to monsters (not inert).
- [ ] **Step 2:** Monster hull traces include monsterclip brushes; player traces ignore them.
- [ ] **Step 3:** Ensure mon-vs-mon slidebox clip remains on AI steps.
- [ ] **Step 4:** Commit `fix(hl1): honour func_monsterclip for AI`

---

### Task 3: Fauna + headcrab leap

**Files:**
- Modify: `HLAI.ZC` (new `HLMON_*`, tables, leap event)
- Modify: `HLEnt.ZC` model table if needed

- [ ] **Step 1:** Add `HLMON_LEECH` (and any other README “inert fauna” that has a simple SDK class worth shipping — leech minimum).
- [ ] **Step 2:** Minimal swim/idle/bite schedules + HandleAnimEvent.
- [ ] **Step 3:** Headcrab leap event: set velocity, airborne state, LeapTouch damage on player contact.
- [ ] **Step 4:** Commit `feat(hl1): leech AI + headcrab leap`

---

### Task 4: MakeDormant + tracktrain hull

**Files:**
- Modify: `HLEntity.ZC` (spawn ~827 area, tracktrain ~10967, train move ~9969)
- Modify: `HLClip.ZC` if hull sizes live there

- [ ] **Step 1:** Implement dormant: skip think/touch/draw until undormant on restore merge; set on global-name dest copies per Deviation note.
- [ ] **Step 2:** Fix tracktrain hull to match SDK sizes / rotating clip so campaign trams don’t sink; cite SDK.
- [ ] **Step 3:** Commit `fix(hl1): MakeDormant + tracktrain hull`

---

### Task 5: Autoaim HUD, gauss glow, RPG spot

**Files:**
- Modify: `HLWeapon.ZC`, `HLHud.ZC` or crosshair draw in weapon file

- [ ] **Step 1:** Publish `hl_wpn_on_target` from autoaim; `HLWeaponDrawCrosshair` swaps to autoaim art when set.
- [ ] **Step 2:** Gauss fire: temp sprites at exit/reflect (reuse particle/sprite helpers).
- [ ] **Step 3:** RPG spot: draw/update spot sprite at `hl_wpn_spot_pos` when `hl_wpn_spot_on` (entity optional; sprite OK under hybrid C).
- [ ] **Step 4:** Commit `feat(hl1): autoaim HUD + gauss glow + RPG spot`

---

### Task 6: Squad audit (light)

**Files:** `HLAI.ZC` hgrunt GetSchedule

- [ ] **Step 1:** Verify OccupySlot gates ranged schedules; if everyone rushes, fix schedule pick to match squadmonster.cpp.
- [ ] **Step 2:** Commit only if behaviour change; else note README fix in Task 7.

---

### Task 7: HLTest + README + stale comments

**Files:** `HLTest.ZC`, `README.md`, comment blocks in `HLAI.ZC` / `HLWeapon.ZC`

- [ ] **Step 1:** HLTest: spawn/fire one AI proj; leech kind resolves; optional monsterclip note.
- [ ] **Step 2:** README Not done: tick Wave A items; fix squad line if recruit works.
- [ ] **Step 3:** Remove stale “player cannot hurt” / zoom FOV half-applied comments.
- [ ] **Step 4:** Commit `test(hl1): Wave A coverage + README`

---

# HL1 Wave B Implementation Plan

> After Wave A. Spec: `docs/superpowers/specs/2026-08-03-hl1-wave-b-design.md`.

**Goal:** Soft-raster FPS via span path, cache hygiene, LightPoint cache, brush cull, blit, bone reuse.

### Task B1: Measure baseline
- [ ] Document `HLRSpeeds` on fixed view before changes.

### Task B2: Unkeyed dlights + mip thrash
- [ ] Grep dlight adds; key all per-frame lights; fix zoom mip0 eviction if cheap.

### Task B3: HLLightPoint cache
- [ ] Grid cache in `HLDraw.ZC`; wire `HLPart`/`HLSpr`.

### Task B4: Brush model cull
- [ ] AABB/backface rejects in `HLDrawBrushModel`.

### Task B5: Span unroll/asm
- [ ] Cached `surf32` inner loop; cvar `r_spanasm` fallback to C.

### Task B6: Clear/blit + studio bone reuse
- [ ] Skip redundant blit scale; cache bones per frame key.

### Task B7: README perf notes + commit
